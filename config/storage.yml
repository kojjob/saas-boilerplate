test:
  service: Disk
  root: <%= Rails.root.join("tmp/storage") %>

local:
  service: Disk
  root: <%= Rails.root.join("storage") %>

# AWS S3 Storage Configuration
# Use bin/rails credentials:edit to set the AWS secrets:
#   aws:
#     access_key_id: YOUR_ACCESS_KEY
#     secret_access_key: YOUR_SECRET_KEY
#     region: us-east-1
#     bucket: your-bucket-name
amazon:
  service: S3
  access_key_id: <%= Rails.application.credentials.dig(:aws, :access_key_id) %>
  secret_access_key: <%= Rails.application.credentials.dig(:aws, :secret_access_key) %>
  region: <%= Rails.application.credentials.dig(:aws, :region) || 'us-east-1' %>
  bucket: <%= Rails.application.credentials.dig(:aws, :bucket) || "saas-boilerplate-#{Rails.env}" %>

# Optional: S3 with CloudFront CDN
# amazon_cdn:
#   service: S3
#   access_key_id: <%= Rails.application.credentials.dig(:aws, :access_key_id) %>
#   secret_access_key: <%= Rails.application.credentials.dig(:aws, :secret_access_key) %>
#   region: <%= Rails.application.credentials.dig(:aws, :region) || 'us-east-1' %>
#   bucket: <%= Rails.application.credentials.dig(:aws, :bucket) %>
#   public: true
#   http_open_timeout: 5
#   http_read_timeout: 5
#   http_idle_timeout: 5
#   upload:
#     cache_control: "public, max-age=31536000"
