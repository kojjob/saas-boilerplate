<!DOCTYPE html>
<html class="h-full">
  <head>
    <title><%= content_for(:title) || "Client Portal - #{@client&.display_name}" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="robots" content="noindex, nofollow">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="min-h-full flex flex-col bg-slate-50 text-slate-900 antialiased">
    <a href="#main-content" class="sr-only focus:not-sr-only focus:absolute focus:top-4 focus:left-4 focus:z-50 focus:px-4 focus:py-2 focus:bg-indigo-600 focus:text-white focus:rounded-lg">
      Skip to main content
    </a>

    <%= render "shared/flash" %>

    <header class="bg-white shadow-sm border-b border-slate-200">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between items-center h-16">
          <div class="flex items-center">
            <%= link_to portal_dashboard_path(token: @client.portal_token), class: "flex items-center" do %>
              <span class="text-xl font-bold text-indigo-600"><%= @account&.name || "Client Portal" %></span>
            <% end %>
          </div>

          <nav class="hidden md:flex items-center space-x-4">
            <%= link_to "Dashboard", portal_dashboard_path(token: @client.portal_token),
                class: "text-sm font-medium #{request.path == portal_dashboard_path(token: @client.portal_token) ? 'text-indigo-600' : 'text-slate-600 hover:text-slate-900'}" %>
            <%= link_to "Invoices", portal_invoices_path(token: @client.portal_token),
                class: "text-sm font-medium #{request.path.include?('/invoices') ? 'text-indigo-600' : 'text-slate-600 hover:text-slate-900'}" %>
            <%= link_to "Estimates", portal_estimates_path(token: @client.portal_token),
                class: "text-sm font-medium #{request.path.include?('/estimates') ? 'text-indigo-600' : 'text-slate-600 hover:text-slate-900'}" %>
            <%= link_to "Projects", portal_projects_path(token: @client.portal_token),
                class: "text-sm font-medium #{request.path.include?('/projects') ? 'text-indigo-600' : 'text-slate-600 hover:text-slate-900'}" %>
          </nav>

          <div class="flex items-center">
            <span class="text-sm text-slate-600">
              Welcome, <span class="font-medium"><%= @client.name %></span>
            </span>
          </div>
        </div>

        <nav class="md:hidden pb-3 flex space-x-4 overflow-x-auto">
          <%= link_to "Dashboard", portal_dashboard_path(token: @client.portal_token),
              class: "text-sm font-medium whitespace-nowrap #{request.path == portal_dashboard_path(token: @client.portal_token) ? 'text-indigo-600' : 'text-slate-600'}" %>
          <%= link_to "Invoices", portal_invoices_path(token: @client.portal_token),
              class: "text-sm font-medium whitespace-nowrap #{request.path.include?('/invoices') ? 'text-indigo-600' : 'text-slate-600'}" %>
          <%= link_to "Estimates", portal_estimates_path(token: @client.portal_token),
              class: "text-sm font-medium whitespace-nowrap #{request.path.include?('/estimates') ? 'text-indigo-600' : 'text-slate-600'}" %>
          <%= link_to "Projects", portal_projects_path(token: @client.portal_token),
              class: "text-sm font-medium whitespace-nowrap #{request.path.include?('/projects') ? 'text-indigo-600' : 'text-slate-600'}" %>
        </nav>
      </div>
    </header>

    <main id="main-content" class="flex-1 py-8">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <%= yield %>
      </div>
    </main>

    <footer class="bg-white border-t border-slate-200 mt-auto">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-4">
        <p class="text-center text-sm text-slate-500">
          Powered by <%= @account&.name || "Subcontractor Command" %>
        </p>
      </div>
    </footer>
  </body>
</html>
