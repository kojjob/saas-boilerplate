<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Estimate #<%= @estimate.estimate_number %> - <%= @estimate.client.name %></title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      font-size: 14px;
      line-height: 1.5;
      color: #1f2937;
      background-color: #f3f4f6;
      padding: 40px;
    }

    .estimate-container {
      max-width: 800px;
      margin: 0 auto;
      background: white;
      box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
      border-radius: 8px;
      overflow: hidden;
    }

    .estimate-header {
      background: linear-gradient(135deg, #7c3aed 0%, #4f46e5 100%);
      color: white;
      padding: 40px;
    }

    .estimate-header h1 {
      font-size: 28px;
      font-weight: 700;
      margin-bottom: 4px;
    }

    .estimate-number {
      font-size: 16px;
      opacity: 0.9;
    }

    .status-badge {
      display: inline-block;
      padding: 4px 12px;
      border-radius: 20px;
      font-size: 12px;
      font-weight: 600;
      text-transform: uppercase;
      margin-top: 16px;
    }

    .status-draft { background: rgba(255,255,255,0.2); }
    .status-sent { background: #3b82f6; }
    .status-viewed { background: #6366f1; }
    .status-accepted { background: #10b981; }
    .status-declined { background: #ef4444; }
    .status-expired { background: #f59e0b; }
    .status-converted { background: #8b5cf6; }

    .estimate-body {
      padding: 40px;
    }

    .info-grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 40px;
      margin-bottom: 40px;
    }

    .info-section h3 {
      font-size: 12px;
      font-weight: 600;
      text-transform: uppercase;
      color: #6b7280;
      margin-bottom: 12px;
      letter-spacing: 0.05em;
    }

    .info-section p {
      margin-bottom: 4px;
    }

    .info-section .company-name {
      font-size: 18px;
      font-weight: 600;
      color: #111827;
    }

    .dates-grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 20px;
      margin-bottom: 40px;
      padding: 20px;
      background: #f9fafb;
      border-radius: 8px;
    }

    .date-item label {
      display: block;
      font-size: 12px;
      font-weight: 600;
      text-transform: uppercase;
      color: #6b7280;
      margin-bottom: 4px;
    }

    .date-item .value {
      font-size: 16px;
      font-weight: 600;
      color: #111827;
    }

    .line-items-table {
      width: 100%;
      border-collapse: collapse;
      margin-bottom: 30px;
    }

    .line-items-table th {
      background: #f9fafb;
      padding: 12px 16px;
      text-align: left;
      font-size: 12px;
      font-weight: 600;
      text-transform: uppercase;
      color: #6b7280;
      border-bottom: 1px solid #e5e7eb;
    }

    .line-items-table th:last-child,
    .line-items-table td:last-child {
      text-align: right;
    }

    .line-items-table td {
      padding: 16px;
      border-bottom: 1px solid #e5e7eb;
    }

    .totals-section {
      display: flex;
      justify-content: flex-end;
    }

    .totals-table {
      width: 280px;
    }

    .totals-table tr td {
      padding: 8px 0;
    }

    .totals-table tr td:first-child {
      color: #6b7280;
    }

    .totals-table tr td:last-child {
      text-align: right;
      font-weight: 500;
    }

    .totals-table .total-row td {
      padding-top: 16px;
      border-top: 2px solid #e5e7eb;
      font-size: 18px;
      font-weight: 700;
      color: #111827;
    }

    .notes-section {
      margin-top: 40px;
      padding-top: 30px;
      border-top: 1px solid #e5e7eb;
    }

    .notes-section h3 {
      font-size: 14px;
      font-weight: 600;
      color: #374151;
      margin-bottom: 8px;
    }

    .notes-section p {
      color: #6b7280;
      white-space: pre-wrap;
    }

    .estimate-footer {
      background: #f9fafb;
      padding: 30px 40px;
      text-align: center;
      border-top: 1px solid #e5e7eb;
    }

    .estimate-footer p {
      color: #6b7280;
      font-size: 13px;
    }

    .validity-notice {
      background: #fef3c7;
      border: 1px solid #fcd34d;
      border-radius: 8px;
      padding: 16px 20px;
      margin-bottom: 30px;
    }

    .validity-notice.expired {
      background: #fee2e2;
      border-color: #fca5a5;
    }

    .validity-notice p {
      color: #92400e;
      font-weight: 500;
    }

    .validity-notice.expired p {
      color: #dc2626;
    }

    @media print {
      body {
        background: white;
        padding: 0;
      }

      .estimate-container {
        box-shadow: none;
        border-radius: 0;
      }

      .no-print {
        display: none !important;
      }
    }
  </style>
</head>
<body>
  <div class="estimate-container">
    <div class="estimate-header">
      <h1>ESTIMATE</h1>
      <p class="estimate-number">#<%= @estimate.estimate_number %></p>
      <span class="status-badge status-<%= @estimate.status %>"><%= @estimate.status.humanize %></span>
    </div>

    <div class="estimate-body">
      <% if @estimate.valid_until < Date.current %>
        <div class="validity-notice expired">
          <p>This estimate expired on <%= @estimate.valid_until.strftime("%B %d, %Y") %>.</p>
        </div>
      <% elsif @estimate.days_until_expiry <= 7 %>
        <div class="validity-notice">
          <p>This estimate is valid for <%= @estimate.days_until_expiry %> more days (until <%= @estimate.valid_until.strftime("%B %d, %Y") %>).</p>
        </div>
      <% end %>

      <div class="info-grid">
        <div class="info-section">
          <h3>From</h3>
          <p class="company-name"><%= @estimate.account.name rescue 'Your Company' %></p>
        </div>

        <div class="info-section">
          <h3>To</h3>
          <p class="company-name"><%= @estimate.client.name %></p>
          <% if @estimate.client.company.present? %>
            <p><%= @estimate.client.company %></p>
          <% end %>
          <% if @estimate.client.email.present? %>
            <p><%= @estimate.client.email %></p>
          <% end %>
          <% if @estimate.client.phone.present? %>
            <p><%= @estimate.client.phone %></p>
          <% end %>
        </div>
      </div>

      <div class="dates-grid">
        <div class="date-item">
          <label>Issue Date</label>
          <p class="value"><%= @estimate.issue_date.strftime("%B %d, %Y") %></p>
        </div>
        <div class="date-item">
          <label>Valid Until</label>
          <p class="value"><%= @estimate.valid_until.strftime("%B %d, %Y") %></p>
        </div>
      </div>

      <table class="line-items-table">
        <thead>
          <tr>
            <th>Description</th>
            <th>Qty</th>
            <th>Unit Price</th>
            <th>Amount</th>
          </tr>
        </thead>
        <tbody>
          <% @estimate.line_items.each do |item| %>
            <tr>
              <td><%= item.description %></td>
              <td><%= item.quantity %></td>
              <td><%= number_to_currency(item.unit_price) %></td>
              <td><%= number_to_currency(item.amount) %></td>
            </tr>
          <% end %>
        </tbody>
      </table>

      <div class="totals-section">
        <table class="totals-table">
          <tr>
            <td>Subtotal</td>
            <td><%= number_to_currency(@estimate.subtotal) %></td>
          </tr>
          <% if @estimate.tax_amount.to_f > 0 %>
            <tr>
              <td>Tax (<%= @estimate.tax_rate %>%)</td>
              <td><%= number_to_currency(@estimate.tax_amount) %></td>
            </tr>
          <% end %>
          <% if @estimate.discount_amount.to_f > 0 %>
            <tr>
              <td>Discount</td>
              <td>-<%= number_to_currency(@estimate.discount_amount) %></td>
            </tr>
          <% end %>
          <tr class="total-row">
            <td>Total</td>
            <td><%= number_to_currency(@estimate.total_amount) %></td>
          </tr>
        </table>
      </div>

      <% if @estimate.notes.present? || @estimate.terms.present? %>
        <div class="notes-section">
          <% if @estimate.notes.present? %>
            <div style="margin-bottom: 20px;">
              <h3>Notes</h3>
              <p><%= @estimate.notes %></p>
            </div>
          <% end %>

          <% if @estimate.terms.present? %>
            <div>
              <h3>Terms & Conditions</h3>
              <p><%= @estimate.terms %></p>
            </div>
          <% end %>
        </div>
      <% end %>
    </div>

    <div class="estimate-footer">
      <p>Thank you for your business!</p>
      <% if @estimate.project.present? %>
        <p style="margin-top: 8px;">Project: <%= @estimate.project.name %></p>
      <% end %>
    </div>
  </div>
</body>
</html>
