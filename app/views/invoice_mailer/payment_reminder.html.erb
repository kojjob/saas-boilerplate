<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <style>
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
      line-height: 1.6;
      color: #333;
      max-width: 600px;
      margin: 0 auto;
      padding: 20px;
    }
    .header {
      border-bottom: 2px solid #eee;
      padding-bottom: 20px;
      margin-bottom: 20px;
    }
    .overdue-badge {
      background: #ef4444;
      color: white;
      padding: 10px 20px;
      border-radius: 5px;
      display: inline-block;
      font-weight: bold;
      margin: 20px 0;
    }
    .reminder-badge {
      background: #f59e0b;
      color: white;
      padding: 10px 20px;
      border-radius: 5px;
      display: inline-block;
      font-weight: bold;
      margin: 20px 0;
    }
    .invoice-details {
      background: #fef3c7;
      padding: 15px;
      border-radius: 5px;
      margin: 20px 0;
      border-left: 4px solid #f59e0b;
    }
    .invoice-details.overdue {
      background: #fee2e2;
      border-left: 4px solid #ef4444;
    }
    .invoice-details table {
      width: 100%;
    }
    .invoice-details td {
      padding: 5px 0;
    }
    .invoice-details .label {
      font-weight: bold;
      color: #666;
    }
    .amount {
      font-size: 24px;
      font-weight: bold;
      color: #f59e0b;
    }
    .amount.overdue {
      color: #ef4444;
    }
    .overdue-notice {
      background: #fee2e2;
      padding: 10px 15px;
      border-radius: 5px;
      color: #991b1b;
      margin: 15px 0;
    }
    .footer {
      margin-top: 30px;
      padding-top: 20px;
      border-top: 1px solid #eee;
      font-size: 14px;
      color: #666;
    }
  </style>
</head>
<body>
  <div class="header">
    <h1>Payment Reminder</h1>
  </div>

  <p>Dear <%= @client.name %>,</p>

  <% if @is_overdue %>
    <div class="overdue-badge">Overdue</div>
    <p>This is a reminder that payment for Invoice <%= @invoice.invoice_number %> is now <strong><%= @invoice.days_overdue %> days overdue</strong>.</p>
  <% else %>
    <div class="reminder-badge">Payment Due Soon</div>
    <p>This is a friendly reminder that payment for the following invoice is due soon.</p>
  <% end %>

  <div class="invoice-details <%= 'overdue' if @is_overdue %>">
    <table>
      <tr>
        <td class="label">Invoice Number:</td>
        <td><%= @invoice.invoice_number %></td>
      </tr>
      <tr>
        <td class="label">Issue Date:</td>
        <td><%= @invoice.issue_date.strftime("%B %d, %Y") %></td>
      </tr>
      <tr>
        <td class="label">Due Date:</td>
        <td><%= @invoice.due_date.strftime("%B %d, %Y") %></td>
      </tr>
      <% if @is_overdue %>
        <tr>
          <td class="label">Days Overdue:</td>
          <td><strong><%= @invoice.days_overdue %> days</strong></td>
        </tr>
      <% end %>
      <tr>
        <td class="label">Amount Due:</td>
        <td class="amount <%= 'overdue' if @is_overdue %>"><%= number_to_currency(@invoice.total_amount) %></td>
      </tr>
    </table>
  </div>

  <% if @is_overdue %>
    <div class="overdue-notice">
      Please arrange payment as soon as possible to avoid any additional late fees or service interruptions.
    </div>
  <% end %>

  <p>If you have already sent payment, please disregard this reminder. If you have any questions or concerns, please don't hesitate to contact us.</p>

  <p>Thank you for your prompt attention to this matter.</p>

  <div class="footer">
    <p>
      <strong><%= @account.name %></strong><br>
      <% if @account.respond_to?(:email) && @account.email.present? %>
        Email: <%= @account.email %><br>
      <% end %>
      <% if @account.respond_to?(:phone) && @account.phone.present? %>
        Phone: <%= @account.phone %>
      <% end %>
    </p>
  </div>
</body>
</html>
