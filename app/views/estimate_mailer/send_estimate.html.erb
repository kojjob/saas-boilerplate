<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <style>
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
      line-height: 1.6;
      color: #333;
      max-width: 600px;
      margin: 0 auto;
      padding: 20px;
    }
    .header {
      border-bottom: 2px solid #eee;
      padding-bottom: 20px;
      margin-bottom: 20px;
    }
    .estimate-details {
      background: #f9f9f9;
      padding: 15px;
      border-radius: 5px;
      margin: 20px 0;
    }
    .estimate-details table {
      width: 100%;
    }
    .estimate-details td {
      padding: 5px 0;
    }
    .estimate-details .label {
      font-weight: bold;
      color: #666;
    }
    .amount {
      font-size: 24px;
      font-weight: bold;
      color: #059669;
    }
    .footer {
      margin-top: 30px;
      padding-top: 20px;
      border-top: 1px solid #eee;
      font-size: 14px;
      color: #666;
    }
    .custom-message {
      background: #ecfdf5;
      padding: 15px;
      border-radius: 5px;
      margin: 20px 0;
      border-left: 4px solid #059669;
    }
    .validity-note {
      background: #fef3c7;
      padding: 10px 15px;
      border-radius: 5px;
      margin: 15px 0;
      font-size: 14px;
      color: #92400e;
    }
  </style>
</head>
<body>
  <div class="header">
    <h1>Estimate from <%= @account.name %></h1>
  </div>

  <p>Dear <%= @client.name %>,</p>

  <% if @custom_message.present? %>
    <div class="custom-message">
      <%= @custom_message %>
    </div>
  <% else %>
    <p>Please find attached your estimate for the requested services.</p>
  <% end %>

  <div class="estimate-details">
    <table>
      <tr>
        <td class="label">Estimate Number:</td>
        <td><%= @estimate.estimate_number %></td>
      </tr>
      <tr>
        <td class="label">Issue Date:</td>
        <td><%= @estimate.issue_date.strftime("%B %d, %Y") %></td>
      </tr>
      <tr>
        <td class="label">Valid Until:</td>
        <td><%= @estimate.valid_until.strftime("%B %d, %Y") %></td>
      </tr>
      <tr>
        <td class="label">Total Amount:</td>
        <td class="amount"><%= number_to_currency(@estimate.total_amount) %></td>
      </tr>
    </table>
  </div>

  <div class="validity-note">
    This estimate is valid until <strong><%= @estimate.valid_until.strftime("%B %d, %Y") %></strong>.
    Please respond before this date to proceed with the quoted services.
  </div>

  <p>If you have any questions about this estimate or would like to discuss the details, please don't hesitate to contact us.</p>

  <p>We look forward to working with you!</p>

  <div class="footer">
    <p>
      <strong><%= @account.name %></strong><br>
      <% if @account.respond_to?(:email) && @account.email.present? %>
        Email: <%= @account.email %><br>
      <% end %>
      <% if @account.respond_to?(:phone) && @account.phone.present? %>
        Phone: <%= @account.phone %>
      <% end %>
    </p>
  </div>
</body>
</html>
